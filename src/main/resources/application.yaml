spring:
  datasource:
    username: AccountTest
    password: 1234567890
    url: jdbc:postgresql://localhost:5432/telegram_bot_tb
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:liquibase/changelog-master.yml